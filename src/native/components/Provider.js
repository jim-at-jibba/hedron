import * as React from 'react';
import { View, Text } from 'react-native';

export default class Provider extends Component {
  render() {
    return (
      <View>
        <Text>From Provider</Text>
      </View>
    )
  }
}
